<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.zzu.langchain4jStarter.mapper.AppointmentRecordMapper">

    <!-- 通用查询映射结果 -->
    <!-- 如果你的Java实体类字段名与数据库列名遵循驼峰到下划线的转换规则，
         MyBatis-Plus通常可以自动映射，这个resultMap可以省略或用于更复杂的映射。
         这里显式定义以供参考和未来扩展。 -->
    <resultMap id="BaseResultMap" type="edu.zzu.langchain4jStarter.entity.AppointmentRecord">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="id_card" property="idCard" />
        <result column="phone_number" property="phoneNumber" />
        <result column="department_name" property="departmentName" />
        <result column="doctor_name" property="doctorName" />
        <result column="appointment_date" property="appointmentDate" />
        <result column="appointment_time_slot" property="appointmentTimeSlot" />
        <result column="appointment_status" property="appointmentStatus" />
        <result column="source" property="source" />
        <result column="notes" property="notes" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

</mapper>